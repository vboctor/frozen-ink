{
  "name": "@frozenink/website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'Static site — no build step required'"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
