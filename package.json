{
  "name": "veecontext",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --build",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "drizzle-kit": "^0.31.10",
    "typescript": "^6.0.2"
  }
}
